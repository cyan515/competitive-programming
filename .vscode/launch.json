{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "LLDB Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/problem.exe",
            "args": [],
            "cwd": "${fileDirname}",
            "stopOnEntry": false,
            "terminal": "integrated"
        }
    ]
}
